{
  "name": "tabx-extension",
  "version": "0.1.0",
  "description": "TabX Chrome Extension - Context-aware tab lifecycle and agent handoff",
  "scripts": {
    "build": "bun run build:ext",
    "build:ext": "tsc --noEmit && bun run bundle",
    "bundle": "bun build src/background/service-worker.ts --outdir dist/background --target browser && bun build src/content/content-script.ts --outdir dist/content --target browser && bun build src/popup/popup.ts --outdir dist/popup --target browser",
    "lint": "bunx tsc --noEmit",
    "typecheck": "bunx tsc --noEmit",
    "test": "bun test tests/"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "typescript": "^5.4.0"
  }
}
